const WordBuilder = () => {
    const styles = {
        container: {
            style: {
                display: 'flex',
                background: 'black'
            }
        }
    }
    return (
        <div>

        </div>
    )
}

export default WordBuilder